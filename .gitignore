# 仓库根 .gitignore —— 排除工具状态与旧版本目录

# └─ Kun / Superdesign 工具运行状态（非项目内容）└─
.kun/
.kun-canvas/

# └─ 旧版本目录（v1 为早期实现；如需保留历史，删除此规则并自行 git add v1/）└─
/v1/

# └─ 内部工作文档（给 AI 的任务提示/核查文档，不对外发布）└─
v2/修改提示词/
v3/修改文档/
# 预留：如日后不想跟踪某目录，在此添加一行（如 results/ ）

# v2 子项目内部的忽略规则见 v2/.gitignore
