# ── Python ──
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 编辑器 / 系统 ──
*.tmp
*.bak
*.orig
*.swp
~$*
.DS_Store
Thumbs.db
*.log

# ── 临时脚本（本仓库约定：_ 开头的调试/检查脚本不入库）──
_*.py
_*.js

# > 说明：results/ 目录规划为入库（仿真结果可作为样例数据保留），
# > 如日后不想跟踪，在此加一行：results/

# 非推送目录（内部文档 / 运行结果）
_internal/
results/

# 运行环境产物（不推送）
.tools/
.kun/
.kun-canvas/
.sshwork/
dsh-deep-whale/
dsh-plugins/
lancecheney-dsh-plugins/
v3.1/

# 敏感凭据：GitHub 2FA 恢复码严禁入库（若已提交请从历史中移除并吊销重新生成）
github-recovery-codes.txt
*.recovery-codes.txt
*recovery*.txt
