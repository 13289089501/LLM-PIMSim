# 02_gpu_pim.yaml — GPU + DRAM-PIM（FFN 放 PIM，FFN 权重驻留 PIM）
experiment:
  name: 02_gpu_pim
  model: llama7b
  seed: 42
  files:
    hardware: ../hardware.yaml
    interconnect: ../interconnect.yaml
    mapping: mapping_pim.yaml
    placement: placement_pim.yaml
  output:
    dir: results/
