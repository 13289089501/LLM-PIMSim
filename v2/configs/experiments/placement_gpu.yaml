# placement_gpu.yaml — GPU-only 专用：所有数据（含权重）都驻留 GPU
default_device: gpu0

initial:
  - data_type: WEIGHT
    devices: [gpu0]
  - data_type: KV_CACHE
    devices: [gpu0]
  - data_type: ACTIVATION
    devices: [gpu0]
  - data_type: TEMPORARY
    devices: [gpu0]
