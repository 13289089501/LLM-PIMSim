# ── Python ──
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 编辑器 / 系统 ──
*.tmp
*.bak
*.orig
*.swp
~$*
.DS_Store
Thumbs.db
*.log

# ── 临时脚本（本仓库约定：_ 开头的调试/检查脚本不入库）──
_*.py
_*.js

# > 说明：results/ 目录规划为入库（仿真结果可作为样例数据保留），
# > 如日后不想跟踪，在此加一行：results/
