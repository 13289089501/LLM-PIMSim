# LLM-PIMSim v4 变更说明（新增：硬件设计系统）

本文件记录 v3.2 → v4 的改动。v4 的核心目标是在原有"用户直接输入硬件参数"的
自定义硬件路径之外，新增一条**"基于真实硬件结构设计并自动生成参数"**的路径：
用户从硬件架构出发（存内计算 / 近存计算）选择存储介质、计算资源、互联方式与
资源组织方式，系统建立硬件结构模型并自动推导出与原有自定义硬件**完全同格式**
的硬件参数，零侵入复用后续的精度检查、算子映射、性能计算与仿真流程。

## 1. 新增系统：core.design_sys（硬件设计系统）

**三阶段流水线**（严格按设计规格，不把用户选择直接转成参数）：

| 阶段 | 结构 | 内容 |
|---|---|---|
| ① 用户设计规格 | `UserDesignSpec` | 总体架构（CIM/NMC）、存储介质、容量与规模、计算资源、资源对应关系、互联方式、部署层级 |
| ② 硬件结构模型 | `HardwareStructureModel` | 通道数、存储阵列/宏块数量、计算资源数量与分布、存储↔计算连接（每条记录互联方式）、每连接有效带宽 = min(存储带宽, 互联带宽, 计算接口带宽) |
| ③ 参数推导 | → `HardwareConfig` 同格式 | 存储类参数来自介质模型；算力/精度能力来自计算机制；效率来自独立规则集 |

**存内计算（CIM）**：
- 存储介质决定基本计算机制，用户无需另选计算单元：
  SRAM → 数字位线/字线计算；DRAM → 存储库/子阵列数字计算；ReRAM → 交叉阵列
  电流累加模拟计算。
- 用户设置总存储容量 + 计算资源密度（低/中/高/自定义），介质模型把密度转换成
  实际阵列数与计算并行度；容量按介质各自的 范围/推荐值/上限/粒度 校验。
- 一个设计 → 一个设备（存储与计算天然耦合）。

**近存计算（NMC）**：
- 存储资源与计算资源分开建模（底层数据结构不写死 2 个上限；UI 建议 ≤2）。
- 每条"存储↔计算"连接记录互联方式（总线 / NoC / 点对点），有效内部带宽 =
  min(存储带宽, 互联带宽, 计算接口带宽)。
- 一个设计 → 单个设备（存储参数来自介质、算力参数来自计算资源）；
  **有效内部带宽写入该设备种类在链路表中的对角线**（同种类自互连条目）。
- 部署层级（通道内混合 / 通道间混合）：通道间混合按"20ns/通道跳"增加读写延迟，
  并按通道数折减有效内部带宽（互联竞争，v1 近似模型）。

**独立算子效率规则**（按用户确认，不改原有效率逻辑）：
- `DESIGN_EFFICIENCY_RULES`：按计算机制类别（SRAM_CIM/DRAM_CIM/RRAM_CIM/
  MAC_ARRAY/CROSSBAR_ARRAY/SIMD_CLUSTER）的 8 类算子基础利用率
  × 精度修正（主执行精度档）× 密度修正。
- 输出仍是 `{op_type: 利用率}` 字典，填入 HardwareConfig.efficiency，
  引擎计算逻辑零改动。

## 2. 两条并行硬件创建路径（共用同一对象与流程）

- 原有路径：`+ 自定义硬件` 弹窗直接输入算力/容量/带宽/延迟。
- 新增路径：`✚ 硬件设计（自动生成参数）` 向导 → 选架构/介质/计算/互联/组织 →
  后端推导 → 加入画布。设计设备同样通过 `serializeState` →
  `build_frontend_custom_hardware` → `HardwareConfig` → `HardwareUnit` 注入，
  与自定义硬件共用精度能力判定（core.precision.HARDWARE_CAPABILITY）、
  算子映射、性能计算与 DES 仿真。

## 3. 新增 API

- `GET /api/design/presets`：预设库（架构/介质/计算资源/互联/密度/部署层级）。
- `POST /api/design/derive`：设计规格 JSON → {device, hardware_config, structure, links}。

## 4. 兼容性小改动（core.hardware_sys）

- 前端单位解析 `_frontend_mem_bytes` / `_frontend_bw_bps` 新增 **PB** 单位支持
  （设计模块可推导出 PB/s 级带宽，如 SRAM-CIM 1.5 PB/s）。
- `build_frontend_custom_hardware` 支持前端透传 `parallelism`（缺省用出厂预设；
  parallelism 为信息字段，不影响引擎计算）。

## 5. 验证

- CIM：SRAM 512MB 中密度 → ≈500 TFLOPS / 1.5 PB/s / 2048 阵列；
  ReRAM 256MB 高密度 → ≈40 TFLOPS。
- NMC：DRAM 8GB + 4×MAC 阵列 + NoC → 131.2 TFLOPS，对角线 256 GB/s；
  通道间混合 2 通道 → 对角线 128 GB/s、延迟 +20ns。
- 端到端：设计设备注入 04_ic_reference 实验并映射算子 → completion_valid=True、
  0 未完成，全部算子可执行。
